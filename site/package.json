{
  "name": "modelsheet-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build:data": "npm --prefix .. run build",
    "build": "npm run build:data && astro build && node scripts/publish-static-data.mjs",
    "dev": "npm run build:data && astro dev",
    "test": "node --test checks/acceptance.mjs",
    "test:dist": "node --test checks/pages-output.mjs",
    "check": "npm test && npm run build && npm run test:dist"
  },
  "devDependencies": {
    "astro": "^7.2.0"
  }
}
